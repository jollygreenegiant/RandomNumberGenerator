<resources>

    <!-- Base application theme. -->
    <style name="DefaultTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DefaultTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CaribbeanSea" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/caribbean_primary_light</item>
        <item name="colorPrimaryDark">@color/caribbean_primary</item>
        <item name="colorAccent">@color/caribbean_accent</item>
        <item name="android:colorButtonNormal">@color/caribbean_accent</item>
    </style>

</resources>
